spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: webengineering

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              org.springframework.data.mongodb.core.MongoTemplate: INFO